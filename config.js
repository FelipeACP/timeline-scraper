
let config = {};
// see README
config.COOKIE = 'YOUR COOKIE HERE';
config.DAY = null;  //should you want a specific day, if null will fetch the whole month
config.MONTH = null; //the month you want to fetch, if null will fetch the current month
config.YEAR = null; //the year you want to fetch, if null will fetch the current year

module.exports = config;
